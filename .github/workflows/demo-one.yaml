name: demo one
on:
  push:
    path:
      - '!README.md'
      - '!docs/**'
      # - '**.js'
    # branchs: [main,staging,develop]
    branchs:
      - 'main'
      - 'release/**'
      - '!dev/**'
jobs:
  demo-one:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODAY=$(date +%y-%m-%d)" >> $GITHUB_ENV
      - run: echo hello world, today is $TODAY
